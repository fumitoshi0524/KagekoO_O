[project]
name = "KagekoO_O"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = ["chromadb>=1.5.7", "dotenv>=0.9.9", "openai>=2.30.0"]

[tool.setuptools]
packages = ["core", "agents", "integrations"]

[dependency-groups]
dev = ["deptry>=0.25.1"]
